{
  "name": "Receipt Tracker Dev",
  "build": {
    "dockerfile": "../Dockerfile",
    "context": ".."
  },
  "remoteUser": "root",
  "mounts": [
    "source=${localWorkspaceFolder},target=/app,type=bind"
  ],
  "workspaceFolder": "/app"
}
