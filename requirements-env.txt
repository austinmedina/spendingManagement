# ============================================
# requirements.txt
# ============================================
flask==3.0.0
python-dotenv==1.0.0
requests==2.31.0
werkzeug==3.0.1
psycopg2-binary==2.9.9
gunicorn==21.2.0

# ============================================
# .env.example (copy to .env and configure)
# ============================================

# Azure Document Intelligence (Form Recognizer)
# Get these from Azure Portal after creating a Document Intelligence resource
AZURE_DOC_INTELLIGENCE_ENDPOINT=https://your-resource.cognitiveservices.azure.com
AZURE_DOC_INTELLIGENCE_KEY=your-api-key-here

# Database Configuration
# Set to 'true' for CSV file storage (testing/simple use)
# Set to 'false' for PostgreSQL (production)
USE_CSV=true

# PostgreSQL Configuration (only needed when USE_CSV=false)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=receipt_tracker
DB_USER=postgres
DB_PASSWORD=your-password

# Flask Configuration
FLASK_ENV=development
FLASK_DEBUG=true
SECRET_KEY=change-this-to-a-random-string-in-production

# Email Configuration (for password reset)
# Gmail example - enable "App Passwords" in Google account settings
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
SMTP_FROM=your-email@gmail.com

# If email is not configured, reset codes will be printed to console

# ============================================
# Allowed Categories (for reference)
# ============================================
# Expenses: Rent, Electric, Investment, Car, Medical, Groceries,
#           Entertainment, Subscriptions, Household, Eating Out, 
#           Shopping, Other
#
# Income: Salary, Freelance, Investment, Gift, Refund, Other
# ============================================
